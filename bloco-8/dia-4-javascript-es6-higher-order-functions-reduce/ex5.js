const assert = require('assert');

const names = [
  'Aanemarie', 'Adervandes', 'Akifusa',
  'Abegildo', 'Adicellia', 'Aladonata',
  'Abeladerco', 'Adieidy', 'Alarucha',
];

function containsA() {
  // escreva seu código aqui
  names.reduce((acc, soma1) => {
    name.split('')
  }, 0)
}

assert.deepStrictEqual(containsA(), 20);